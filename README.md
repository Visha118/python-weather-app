# Weather CLI App (Python)

A simple command-line Python app that fetches real-time weather for any city using the Open-Meteo API.

## Features
- Search weather by city name
- Shows temperature and windspeed
- Uses free API (no key needed)

## How to Run

```bash
pip install -r requirements.txt
python weather.py
